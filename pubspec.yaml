name: the_algorithms_dart

repository: https://github.com/TheAlgorithms/Dart

dependencies:
  test: ^1.15.4
  coverage: ^1.6.0
  welltested_annotation: ^1.0.1

dev_dependencies:
  stack: ^0.2.1
  build_runner:
  mockito:

environment:
  sdk: ">=2.10.0 <3.0.0"
